# 观心 Labs 佛典项目维护规则

## 项目范围

- 本仓库用于整理佛典原文、注音读诵、白话通解、义理解析和修订记录。
- 维护者为观心研习社，维护署名为泓贤。
- 项目以“通俗、易学、能持续读下去”为成果目标，不是纯粹的文字或文学勘验；校勘用于守住经文可靠性，不替代学习者的阅读需要。

## 内容边界

- 经文原文不得因排版、注音或翻译需要而直接改写。
- 注音读诵、白话通解和义理解析必须使用独立文件。
- 注音读诵稿只标注生僻字、多音字、易误读词及必要梵语音译，不制作全文逐字拼音。
- 佛教专名的本经写法必须保留；已可靠对应现代汉传通称时，可在衍生文件首次出现处按项目规则标示通称和读音。鸠摩罗什译《妙法莲华经》可作为常用汉传专名写法与读法的参照，不得据此覆盖其他译本的原文字形。
- 不得将未经校对的内容标记为正式完成。
- 经文增删、品目调整和异文处理必须注明依据，不能凭推测修正。

## 文件管理

- 不自动移动、删除、拆分或批量重命名经文文件。
- 不提交重复副本、临时文件和 Obsidian 个人工作区状态。
- 空白占位文件不得被误报为已完成内容。
- 中文文件名必须正常显示，不修改全局 Git 配置。

## Git 管理

- `guanxinlabs/fodian-project` 的 GitHub `main` 是本项目唯一正式成果终点；已核定且通过必要检查的实际修改，默认直接提交并同步到该仓库，无需逐次提醒或再次征询。
- 明确要求“只查看”“只核验”“不修改”“暂不同步”或内容仍属待核/草稿时，不创建提交、不同步。
- Obsidian 仅为维护者个人查看与桌面工作入口，不是第二成果源；本工作流不写入 Obsidian 个人工作区状态，也不提交 `.obsidian/workspace*.json`。
- 一类内容一个原子提交。
- 经文内容、校对记录和仓库配置不得混在同一提交。
- 禁止使用 `git add -A` 或 `git add .`。
- Commit 前必须执行 `git diff --check` 并检查暂存文件清单。
- 未经明确授权，不执行删除、覆盖、Reset、历史改写或强制 Push。


## 佛典项目启动与结构继承

在本仓库处理佛典内容前，必须先读取：

1. `00 佛典项目-Codex工作说明.md`
2. `00 佛典研习基本原则与学习方法.md`
3. `00 佛典原文母本校勘与同步规则.md`
4. `00 佛典研习输出格式规范.md`
5. `00 佛典项目当前状态与共识.md`
6. 当前经典的 `01 经文原文`、`06 经义研习`、`99 修订记录` 与目标文件。

已确认的目录、文件名、文件用途、标题层级、字段顺序、段落编号和学习单元边界必须继承。任何格式、编号或结构变更，先更新相应 `00` 规则并取得维护者确认，再调整实际内容。
